# โจทย์ anajak-car (เริ่มใหม่ 2026-08-31)

> ไฟล์เดียวของโปรเจกต์นี้ — เก็บ "โจทย์ที่เบสเคาะ" + "ของจริงที่สำรวจแล้ว" · ยังไม่ใช่สเปกเทคนิค

## เว็บนี้คืออะไร
สารานุกรมรถ + ราคา + tier list/meta · แรงบันดาลใจ **dotabuff / prydwen**
คนใช้ = คนที่อยากรู้ลึกเรื่องรถ (ไม่ใช่คนหารถซื้อทั่วไป และไม่ใช่คนอ่านรีวิว)

## เบสเคาะแล้ว (2026-08-31)
1. **tier list = ผสม** — คะแนนจากตัวเลขจริง + คำอธิบายในภาษาคนว่าทำไม · **และต้องมี community โหวตด้วย**
2. **รอบแรกลึกแบรนด์เดียว = Mercedes-Benz** — ครบทุกรุ่น ทุกโฉม ทุกรุ่นย่อย ราคาครบ
3. แหล่งข้อมูล = ให้ Nami สำรวจก่อน (ผลอยู่ข้างล่าง)

## ปัญหาแกนที่ต้องแก้ให้ได้ (ไม่แก้ = ได้แค่หน้าตา dotabuff)
dotabuff/prydwen มีคนกลับมาทุกวันเพราะ **ข้อมูลเปลี่ยนทุกวัน** (patch ขยับ ตัวไหนแข็ง/อ่อน)
รถไม่มี patch — สเปกรถนิ่งสนิท → ถ้าเว็บมีแต่สเปก คนเข้าครั้งเดียวแล้วไม่กลับมา
**สิ่งที่ขยับทุกวันจริงในโลกรถ (ยืนยันจากการสำรวจแล้ว):** ราคาประกาศขายมือสอง · จำนวนคนดูประกาศแต่ละรุ่น · รถเข้าใหม่/โฉมใหม่ · โปรลดราคา/รถ Demo
→ **นี่คือชีพจรของเว็บ ไม่ใช่สเปก**

## แหล่งข้อมูล — สำรวจของจริงแล้ว 2026-08-31 (เปิดดูเองทุกเว็บ ไม่ได้เดา)

| แหล่ง | ได้อะไร | ดึงได้ไหม |
|---|---|---|
| `mercedes-benz.co.th` (ทางการ) | 25 รุ่นในไทย + ราคาเริ่มต้นสด + ขุมพลัง (ไฟฟ้า/ไฮบริด/ดีเซล/เบนซิน) + หมวดตัวถัง | ✅ เปิดได้ · ราคาสดที่สุด |
| `taladrod.com` | ประกาศมือสอง + **breadcrumb "แบรนด์ » โฉม(W206 ปี22-ปัจจุบัน) » รุ่นย่อย » ปี"** + ราคา + **จำนวนคนดู** + เวลาลงประกาศ | ✅ เปิดได้ ไม่กันบอท · กรองยี่ห้อได้ |
| `autoinfo.co.th` | สเปกละเอียดรายรุ่นย่อย: ซีซี/แรงม้า/เกียร์/ขับเคลื่อน/กว้าง-ยาว-สูง/ฟีเจอร์ (ABS ถุงลม cruise lane keeping ฯลฯ) | ⚠️ เปิดได้ แต่ **ข้อมูลค้างปี 2022** (C-Class อัปเดตล่าสุด 24 Apr 2022) |
| `one2car.com` | ประกาศมือสองเจ้าใหญ่สุด | ❌ กันบอท (Cloudflare) เปิดไม่ได้ |
| Pantip / Lemon8 / บล็อก | ค่าเซอร์วิสจริง ปัญหาที่เจอบ่อย ค่าใช้จ่ายรายปี | ⚠️ มีจริงแต่กระจัดกระจาย ไม่มีโครงสร้าง — ต้องเรียบเรียงเอง/ให้ community เติม |

**สิ่งที่ค้นพบสำคัญ:** เว็บรถไทยที่มีสเปกละเอียดที่สุด (autoinfo) ข้อมูลค้างมา 4 ปี
→ ช่องว่างจริงมีอยู่ · แต่แปลว่า **ข้อมูลลึกต้องเก็บเอง ไม่มีใครแจกให้ครบ**

**รหัสตัวถัง (W206 / W247 / W223) โผล่ในทั้งชื่อประกาศและ breadcrumb ของ taladrod**
→ ใช้เป็นกุญแจผูก "โฉม" ได้เลย — ตรงกับที่สารานุกรม Benz ต้องมีพอดี

## ยังไม่เคาะ (รอเบส)
- ดึงข้อมูลจากเว็บคนอื่นอัตโนมัติ = ทำหรือไม่ทำ (เรื่องเงื่อนไขการใช้เว็บของเขา)
- หน้าตา/โครงเว็บ — **ยังไม่วาด** จนกว่าโจทย์ข้างบนจะนิ่ง (รอบก่อนตายเพราะวาดก่อนล็อกโจทย์)
- คะแนน tier คำนวณจากตัวเลขอะไรบ้าง (ค่าเสื่อม? ราคาต่อแรงม้า? ความนิยมจากยอดวิว?)

## บทเรียนการเก็บข้อมูล (2026-08-31 — ลงมือจริงแล้ว)
- **โครงสารานุกรมได้มาฟรี**: taladrod เปิดไฟล์รายชื่อ `MMT.js` + `MBD.js` เป็นไฟล์สาธารณะ (เบราว์เซอร์ทุกคนโหลดอยู่แล้ว) → แปลงเป็น **รุ่น → โฉม → รุ่นย่อย** ครบทั้งแบรนด์ในไม่กี่วินาที ไม่ต้องไล่เปิดทีละหน้า → `scripts/build-taxonomy.mjs`
- **ราคาอยู่ในหน้าเว็บอยู่แล้ว**: แต่ละหน้าฝัง `var SchDataJSON={...}` มีครบทั้ง ปี/รุ่นย่อย/ราคา/**ราคาก่อนลด**/ยอดวิว/ขายแล้วหรือยัง — และมีมากกว่าที่หน้าเว็บแสดง (หน้าโชว์ 26 คัน ใน JSON มี 67)
- ต้องส่ง header ให้ครบเหมือนเบราว์เซอร์จริง (`Accept-Language`, `Sec-Fetch-*`) ไม่งั้น server ส่งหน้าเปล่ากลับมา
- ⚠️ **เว็บใช้ AWS WAF กันบอท** — ขอเร็ว 1.2 วินาที/หน้า โดนหน้า "verify that you're not a robot" ตั้งแต่หน้าที่ ~30 · **ปลดเองหลังพัก ~4 นาที**
  → สคริปต์จึงตั้ง **เว้น 15 วินาทีต่อหน้า + เจอหน้ากันบอทแล้วถอยพัก 5 นาที** (ถอยเมื่อเว็บขอ ไม่หาทางหลบ — ตามที่เบสเคาะว่า "ดึงสุภาพ")
  → เก็บครบ 87 โฉม = ราว 22 นาทีต่อรอบ · เหมาะกับรอบวันละครั้ง ไม่เหมาะกับดึงสด
- **ข้อควรระวังถ้าจะพึ่งแหล่งนี้ระยะยาว**: เว็บเจ้าของข้อมูลตั้งใจกันการเก็บอัตโนมัติ · เขาเปลี่ยน/บล็อกได้ทุกเมื่อ · ระยะยาวควรมีข้อมูลของเราเอง (ชุมชนช่วยเติม/ยืนยัน) ไม่ผูกชีวิตไว้กับเว็บเดียว

## กฎที่ต้องฝังในเว็บตั้งแต่วันแรก (เจอกับตัว 2026-08-31)
**ห้ามรวมรถคนละโฉมเข้าด้วยกันเพื่อให้ตัวอย่างเยอะขึ้น — ตัวเลขจะโกหกทันที**
เคสจริง: E-Class อายุ 2-4 ปี ถ้ารวมทุกโฉม ได้ "เหลือ 41% ของราคาป้ายแดง" ฟังดูน่าตกใจ
แต่พอแยกโฉม → W213 (โฉมเก่าที่ตกรุ่นแล้ว) 32 คัน กลาง 1.40 ล้าน เป็นตัวลากค่ากลางลง
ส่วน W214 ซึ่งเป็นโฉมที่ขายอยู่จริง เหลือ **86%** — คนละเรื่องกันเลย
→ ทุกตัวเลขบนเว็บต้องผูกกับ **รุ่น + โฉม + ช่วงอายุ** เสมอ และต้องบอกว่าคิดจากกี่คัน
→ ตัวอย่างน้อยกว่า 10 คัน = แสดงได้แต่ต้องเขียนกำกับว่ายังสรุปไม่ได้
