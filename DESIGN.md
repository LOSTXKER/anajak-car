# แนวทางหน้าตาเว็บ anajak-car

> ทุกข้อข้างล่างมาจาก **ข้อมูลที่เก็บได้จริง** ไม่ใช่รสนิยม — ถ้าข้อมูลเปลี่ยน หน้าตาต้องเปลี่ยนตาม

## คนใช้คือใคร → หน้าตาต้องเป็นแบบไหน
คนที่อยากรู้ลึกเรื่องรถ — **มาอ่านตัวเลข ไม่ได้มาชมรูป**
→ **ตารางมาก่อนการ์ด** · เห็นข้อมูลเยอะในหน้าเดียว กวาดตาเทียบได้ · ไม่มี hero รูปรถเต็มจอ

**ภาษาหน้าตา = Notion** (เบสเคาะ 2026-08-31 — เปลี่ยนจากธีมมืดแบบ dotabuff ที่ทำรอบแรก)
- พื้นขาว · sidebar เทาอ่อนซ้าย · เส้นแบ่งบางมาก · **ไม่มีเงา ไม่มี gradient ไม่มีสีจัด**
- หน้าเหมือนเอกสาร: อิโมจินำหัวเรื่อง · หัวเรื่องใหญ่ · เนื้อหาไหลลงมาเป็นบล็อก
- **callout** (กล่องเทาอ่อนมีอิโมจิ) ใช้บอกที่มา/ข้อจำกัดของข้อมูล — วางไว้บนสุดเสมอ
- ตาราง = database view: หัวคอลัมน์ตัวเล็กสีเทา แถว hover เป็นเทาจาง
- ป้าย (chip) พื้นพาสเทลมุมมน 3px ตัวอักษรเข้ม
- ฟอนต์ Noto Sans Thai + Inter · ตัวเลขใช้ tabular-nums ให้ตรงคอลัมน์
- **ชื่อรุ่นห้ามเป็นตัวใหญ่ล้วนและห้ามย่อจนไม่รู้ว่ารุ่นอะไร** — "C-Class" ไม่ใช่ "C-CLASS" และไม่ใช่ "C"
  (พลาดมาแล้วรอบแรก: ตัด -CLASS ออกจนเหลือตัวเดียว อ่านไม่ออก) · ตัวย่อและรหัสคงไว้ (AMG, W206, 4MATIC)

## 3 กฎที่ห้ามผิด (เว็บนี้ขายความแม่น ผิดข้อเดียวคือจบ)
1. **ทุกตัวเลขต้องบอกว่าคิดจากกี่คัน** — "เหลือ 66%" ต้องมี "จาก 58 คัน" ติดเสมอ
   ต่ำกว่า 10 คัน = แสดงได้แต่ต้องหรี่สีลง + เขียนว่ายังสรุปไม่ได้
2. **ห้ามรวมคนละโฉม** — ทุกตัวเลขผูกกับ รุ่น + โฉม + ช่วงอายุ (เหตุผล: SPEC.md — เคสจริงที่เกือบทำให้เว็บโกหก)
3. **บอกที่มาและวันที่เก็บทุกหน้า** — "ราคาที่ผู้ขายตั้ง ไม่ใช่ราคาปิดจริง · เก็บ 31 ส.ค. 2026"

## หน้าแรกทำหน้าที่อะไร
**โชว์สิ่งที่เพิ่งขยับ** ไม่ใช่โชว์ว่าเรามีข้อมูลเยอะ
เพราะสเปกรถไม่เปลี่ยน แต่ราคาเปลี่ยน — ของที่ทำให้คนกลับมาคือ "อะไรขยับตั้งแต่ครั้งก่อน"
- รถที่เพิ่งลดราคา (ตอนนี้มี 72 คัน)
- อันดับค่าเสื่อม (ตัวไหนกำลังทิ้งดิ่ง ตัวไหนยืนราคา)
- ของในตลาดตอนนี้มีกี่คัน แยกตามรุ่น

## โครงหน้า (ตามข้อมูลที่มีจริง)
```
/                  ชีพจรวันนี้ + อันดับค่าเสื่อม + ทางเข้ารุ่น
/benz              รุ่นทั้งหมด 34 รุ่น — ของในตลาด / ช่วงราคา / โฉมที่มี
/benz/c-class      โฉมทั้งหมด (W202→W206) — ราคาแต่ละโฉม + ค่าเสื่อม
/benz/c-class/w206 รุ่นย่อยในโฉมนี้ + ประกาศจริงที่ขายอยู่
```
URL อ่านออก เดาได้ แชร์ได้ (แบบ dotabuff)

## สี
- **สีใช้สื่อความหมายเท่านั้น ห้ามใช้เพราะสวย** — แดง = ค่าเสื่อมหนัก/ราคาลด · เหลือง = กลางๆ · เขียว = ยืนราคาดี · เทา = ตัวอย่างน้อย ยังเชื่อไม่ได้
- ใช้โทนพาสเทลแบบ Notion (พื้นอ่อน ตัวอักษรเข้ม) ไม่ใช่สีสด
