# CARMETA — DESIGN

> อ่านก่อนงาน UI · ไฟล์นี้**จงใจบาง** — visual ทั้งหมด (สี/ฟอนต์/layout/โครง nav/ความหนาแน่น) ยังไม่ถูกล็อก = **ของรอบ redesign ตัดสิน** ไม่มีกฎล่วงหน้า

## ⚠️ กติกาของไฟล์นี้ (กัน design space หด — เจ็บมาแล้ว 2 รอบ 07-19→21)
- เก็บเฉพาะ 3 อย่าง: **ตัวตนผลิตภัณฑ์บน UI · ทิศที่เบสชี้ · บทเรียนเทคนิค** — ไม่เก็บกฎ visual ไม่เก็บคำห้าม ไม่เก็บรสนิยมของทิศที่ตายแล้ว
- ฟีดแบ็กรายรอบ ("ไม่เอา X") = ปรับงานรอบนั้น + จดลง PROGRESS.md/git — **ไม่จดเป็นกฎที่นี่** · จะเสนอของที่เบสเคยปัด → เสนอได้ แต่บอกว่าเคยปัดเมื่อไหร่
- **ทิศใหม่/redesign → ทำหน้าเทียบ 2-3 ทิศให้เบสเห็นภาพจริงก่อน แล้วค่อย implement ทิศที่เบสเลือก**
- สไตล์/token ที่ตกลงกันแล้วหลังเบสเคาะ mockup = อยู่ในโค้ด (`globals.css`) เป็น ground truth — ไฟล์นี้ไม่ก๊อปมาแช่เป็นกฎ

## 🧭 ทิศทาง (เบสชี้ 2026-07-21 — input หลักของรอบ redesign)
เบสส่ง reference 7 เว็บ "อยากได้ประมาณนี้":
- https://www.dotabuff.com · https://dota2protracker.com · https://op.gg/lol/champions · https://tracker.gg/lol (เกม stat-tracker)
- https://www.prydwen.gg/star-rail (game wiki/DB)
- https://www.coinbase.com/explore · https://coinmarketcap.com (ตารางตลาด: ราคา + %เปลี่ยนแปลง + sparkline)

**สิ่งที่ 7 เว็บมีร่วมกัน (observation ให้ mockup ใช้ — ไม่ใช่ข้อบังคับ):** ตาราง/กริดข้อมูลแน่นเป็นพระเอกตั้งแต่ fold แรก · ตัวเลข + %change + sparkline ต่อแถว · หลายสีตามหน้าที่ (เขียว/แดง/tier) · dark theme เป็นหลัก · nav utilitarian · หน้า entity มี stat block ใหญ่
### ✅ เคาะแล้ว + implement แล้ว (M22 · 2026-07-22) — สถานะปัจจุบันของเว็บ
เส้นทางกว่าจะเจอ (ผ่านหลาย mockup): tracker/market แน่นๆ ตาม reference → เบส **"รกไป"** · Apple minimal → **"โล่งไป"** · สรุปเบสชี้หน้าจริงเดิม (M21) ว่า **"อยากได้หน้าแรกประมาณนี้"** = search-first landing ที่ต้อนรับ ไม่ใช่ dashboard แน่น
**ดีไซน์ที่ลงโค้ดจริงแล้ว (branch `redesign/m22-carmeta-light-kanit`):**
- **ฟอนต์ Kanit** ทั้งเว็บ (next/font · `--font-kanit`) · ตัวเลข tabular = Geist Mono ผ่าน `.tnum` (Kanit ไม่มี tnum จริง)
- **light-only** (เบสเลือก 2026-07-22) — ตัด dark + theme toggle: เว็บโลโก้แบรนด์เยอะไม่เหมาะธีมมืด (โลโก้จม/ชิปขาวไม่สวย/โมโนโครม Ford เพี้ยน) เหมือนเว็บรถชั้นนำ (KBB/CarGurus/Kayak) · **dark พักไว้** (ถ้าจะทำต้องทำ dark-variant โลโก้รายแบรนด์)
- **หน้าแรก**: hero **typewriter** (ฮุค "เช็กก่อนซื้อ" คงที่ + บรรทัด 2 พิมพ์วนเปลี่ยน คำท้ายสี accent · `hero-headline.tsx` · reduced-motion+aria) → search-first → แถวโลโก้แบรนด์จริง (Toyota + เร็วๆนี้) → ตารางสะอาด
- **ตาราง (`car-database-explorer`)**: จุดสีตามหมวดขุมพลัง (diesel/petrol/hybrid/ev token) + ข้อความคู่เสมอ (a11y) · หัวเรียงได้ทุกคอลัมน์ (⇅ จาง · เพิ่ม year/variants) · ตัด "เริ่มต้น"/โลโก้ต่อแถว/ป้าย "ขายอยู่" (default = noise) · ราคา/ปี/จำนวน = `.tnum`
- token สี: คง accent Royal #2f56c9 + เพิ่ม `--pt-diesel/petrol/hybrid/ev` (สื่อ "หมวด" ไม่ใช่คะแนนรถ) · ทุกหน้า (home/cars/brands) รับผ่าน globals อัตโนมัติ
- verify: lint+tsc+build ผ่าน · เปิดจริง port 3105 + screenshot 3 หน้า
**ยังไม่ทำ (backlog):** merge→main/deploy · ชิปงบด่วน · ป้าย "ปรับราคาล่าสุด/ใหม่" (ใช้ประวัติ append-only) · เทียบรุ่น · หน้ารุ่นใส่ data รวยขึ้น (strip-plot การกระจาย) · mobile ตาราง→การ์ด · รูปจริง Champ/Revo
**~ขั้นต่อไปเดิม~** (ทำแล้ว): ทำ mockup เทียบ → เบสเลือก → implement
- asset ที่มี: โลโก้ CARMETA ตระกูลน้ำเงิน Royal (#2f56c9) — ใช้/ไม่ใช้/ปรับ ให้รอบ mockup ตัดสิน

## ตัวตนผลิตภัณฑ์บน UI (ส่วนเดียวที่ต้องอยู่ในทุกดีไซน์ — มาจาก PRODUCT.md)
ทุกตัวเลข/ข้อเท็จจริงสำคัญ ต้องแสดง (หรือเข้าถึงได้ทันที): **Source** (มาจากไหน+ลิงก์) · **Freshness** (ข้อมูล ณ วันที่) · **Confidence** (สูง/กลาง/ต่ำ) · **Sample** (ราคามือสอง = "จาก N ประกาศ") · **สถานะข้อมูล** (ไม่มี = บอกว่าไม่มี ไม่โชว์ 0 ปลอม — `DataStatus`) · แยกชนิดราคาให้เห็นชัด (ป้ายมือหนึ่ง ≠ โปร ≠ ประกาศมือสอง ≠ ซื้อขายจริง)
— จะแสดง**หน้าตายังไง**เป็นเรื่องของดีไซน์แต่ละทิศ · กฎเหล็กผลิตภัณฑ์ (ไม่จัดอันดับรวม ฯลฯ) → `PRODUCT.md`
มาตรฐานขั้นต่ำเสมอ: responsive · เข้าถึงได้ (contrast/aria)

## แนวนำเสนอข้อมูลที่เคยพิสูจน์แล้วเวิร์ก (วัตถุดิบ — หยิบใช้ได้ ไม่บังคับ)
หนึ่งหน้าหนึ่งพระเอก · บันไดราคารุ่นย่อย +ส่วนต่างจากขั้นก่อน · สเปกย่อเป็นประโยคเดียว · สาร/ค่าที่ซ้ำหลายจุดยกไปพูดครั้งเดียว

## สถานะโค้ด (จุดตั้งต้น — รอ redesign ทับ)
- main = **M21**: hero search-first + ตารางรุ่นรถ + หน้ารายละเอียด/บันไดราคา + ADAS · การตัดสินใจดีไซน์ยุคก่อน (07-19→20) → git log/PROGRESS (ไม่ใช่กฎอีกต่อไป)
- งาน M22-M24 (โครง sidebar prydwen + Apple polish) → branch `archive/m22-m24-sidebar-apple` (กันหาย · ฟื้นได้ถ้าทิศใหม่ต้องการ)

## ⚠️ บทเรียนเทคนิค/เครื่องมือ (ข้อจำกัดจริงที่วัดได้ — ไม่ใช่รสนิยม)
- **Chrome headless บังคับความกว้างขั้นต่ำ 500px** — `--window-size=390` ได้ viewport 500 จริง แล้ว crop เหลือ 390 → ภาพดู "เบ้ซ้าย-ตัดขวา" ทั้งที่ layout ไม่พัง · ตรวจ mobile < 500px ใช้ **iframe harness** (หน้า ≥500 มี `<iframe width=390>`) หรือ CDP `Emulation.setDeviceMetricsOverride`
- ก่อนสรุป "layout พัง" จาก screenshot: เช็ค `innerWidth` จริงก่อนเสมอ
