# สถานะงาน anajak-car

อัปเดต: 2026-08-31 · branch `reset/start-over` (ยังไม่ merge เข้า main — main ยังเป็นเว็บเก่าที่ถูกล้าง)

## ตอนนี้อยู่ตรงไหน
เริ่มใหม่จากศูนย์ตามที่เบสสั่ง แล้วเดินมาถึง **เว็บที่ใช้งานได้จริง 102 หน้า จากข้อมูลจริง**

ทำเสร็จแล้ว:
1. **โจทย์ชัด** → `SPEC.md` (สารานุกรมรถ + ราคา + tier list ผสมข้อมูล/ความเห็น/โหวต · รอบแรกเจาะ Mercedes-Benz)
2. **เก็บข้อมูลเอง** → `scripts/build-taxonomy.mjs` + `scripts/collect-listings.mjs`
   - โครง 34 รุ่น / 87 โฉม / 601 รุ่นย่อย · ประกาศขายจริง 1,813 คัน ครบทุกโฉม
   - ราคาป้ายแดง 25 รุ่นจากเว็บ Mercedes-Benz ทางการ
3. **เว็บ** (Next.js 16) — หน้าแรก / รุ่นทั้งหมด / รายรุ่น / รายโฉม · **หน้าตาแนว Notion** พื้นขาว sidebar ซ้าย callout บอกที่มา ตารางแบบ database (เบสเคาะ 08-31 · กฎใน `DESIGN.md`)

## เหลือทำ (เรียงตามที่ควรทำก่อน)
1. **รอเบสดูเว็บแล้วชี้เป็นจุด** ว่าตรงไหนไม่โดน — ยังไม่ merge เข้า main จนกว่าเบสจะพอใจ
2. **ยังไม่มีในเว็บ แต่เบสสั่งไว้ในโจทย์:** tier list ที่มีคำอธิบายภาษาคน + ระบบให้คนโหวต (ตอนนี้มีแต่ตัวเลขค่าเสื่อม)
3. **ตั้งรอบเก็บข้อมูลประจำ** — ตอนนี้ต้องสั่ง `npm run collect` เอง (ราว 22 นาที) · ถ้าไม่เก็บซ้ำ หน้า "เพิ่งลดราคา" จะค้างอยู่กับที่
4. **เก็บราคาย้อนหลัง** — ตอนนี้เก็บทับไฟล์เดิม ยังดูไม่ได้ว่าราคาขยับยังไงข้ามสัปดาห์ (ซึ่งคือหัวใจของ "ชีพจร")
5. แบรนด์ที่ 2 — สคริปต์รองรับแล้ว เปลี่ยนแค่ชื่อแบรนด์
6. ข้อมูลที่ยังไม่มีเลย: สเปกรถ (แรงม้า/มิติ/ออปชั่น) · ค่าซ่อมบำรุง · ปัญหาที่เจอบ่อย

## ข้อควรระวังของโปรเจกต์นี้
- ข้อมูลมาจาก taladrod ซึ่ง**กันบอทด้วย AWS WAF** — สคริปต์ตั้งให้ถอยเมื่อเขาขอ (เว้น 15 วิ/หน้า) ห้ามเร่ง
- ทุกตัวเลขบนเว็บต้องผูกกับ รุ่น+โฉม+ช่วงอายุ และบอกว่าคิดจากกี่คัน (เหตุผล: `SPEC.md` — เคสที่เกือบทำให้เว็บโกหก)
